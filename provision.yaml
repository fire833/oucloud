
# Run all the roles
---
- name: Proxmox Base Role Provision
  hosts: proxmox
  tags: proxmox
  become: true
  become_user: root
  roles:
  - proxmox

- name: Router Base Role Provision
  hosts: router
  tags: router
  become: true
  become_user: root
  roles:
  - router
